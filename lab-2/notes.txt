#utworzenie biblioteki statycznej z rozszerzeniem .a
	gcc -c bibl1.c
	ar rcs libbibl1.a bibl1.o
#utworzenie blblioteki dynamicznej z rozszerzeniem .so
	gcc -fPIC -c bibl1.c
	gcc -shared  bibl1.o -o libbibl1.so
#utworzenie programu
	gcc main.c libbibl1.a -o main_stat
	gcc main.c -L. -lbibl1 -Wl, -rpath=,. -o main_wspoldz #dodatkowa sciezka do bibl .
	gcc main2.c -o main_ladow_dynam -ldl

